/**
 * Stage 5: 최종 JSON 출력 및 3대 핵심 검증 프롬프트 빌더
 * 환각 방지를 위한 동적 날짜 주입
 */

import type { DateContext } from './types';

/**
 * Stage 5 프롬프트 생성
 *
 * @param context - 날짜 컨텍스트 (동적 주입)
 * @returns Stage 5 프롬프트 문자열
 */
export function getStage5JsonOutput(context: DateContext): string {
  const { today, targetDate, searchFormats, forbiddenYearThreshold } = context;

  return `━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STAGE 5: 최종 JSON 출력 및 3대 핵심 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 목표: JSON 생성 + 3가지 핵심 사항 최종 검증

🚨🚨🚨 최우선 임무: JSON 출력 전 반드시 검증 🚨🚨🚨

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📅 날짜 컨텍스트 (사전 계산됨 - 신뢰하세요)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔴 오늘 날짜: ${today.korean}
🔴 분석 기준일 (전일 거래일): ${targetDate.korean}
🔴 ISO 형식: ${targetDate.iso}
🔴 숫자 형식: ${targetDate.numeric}

⚠️ 위 날짜는 시스템에서 자동 계산된 정확한 값입니다 (주말/공휴일 제외 완료).
⚠️ 전일 영업일을 직접 계산하지 마세요 — 위 날짜를 그대로 사용하세요.
⚠️ ${forbiddenYearThreshold}년 이전 데이터 절대 사용 금지!

📌 검색용 날짜 포맷 (복사해서 사용):
   - Naver/Daum: "${searchFormats.naverStyle}"
   - KRX: "${searchFormats.krxStyle}"
   - ISO: "${searchFormats.isoStyle}"
   - 점 형식: "${searchFormats.dotStyle}"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【STEP 1】 이전 STAGE 결과에서 3개 종목 + 예비 후보 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이전 STAGE 4 결과를 읽고 다음 정보 추출:

상위 3개 종목:
- ticker (예: KOSPI:005930 또는 KOSDAQ:035760)
- name / close_price / rationale / signals

예비 후보 (4위~10위):
- STAGE 5 검증 실패 시 교체용으로 대기
- 교체 시에도 반드시 아래 STEP 2~3 검증을 통과해야 함

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【STEP 2】 🔴 핵심 검증 #1: 종목 실존 여부 (MANDATORY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚨 각 종목이 실제로 존재하는 상장 종목인지 확인 (Google Search 활용)

종목 1 검증:
✅ "[종목명] [6자리코드] 주가 site:finance.naver.com"
✅ "[6자리코드] 종목 정보 site:data.krx.co.kr"

IF 검색 결과가 없거나 "상장폐지", "거래정지" 등장:
   ❌ 해당 종목은 사용 불가
   ❌ 예비 후보로 교체 후 재검증

종목 2 검증: (동일 프로세스 반복)
종목 3 검증: (동일 프로세스 반복)

✅ 3개 종목 모두 실존 확인 완료해야만 다음 단계 진행

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【STEP 3】 🔴 핵심 검증 #2: 전일 종가 정확성 (MANDATORY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚨 각 종목의 전일 종가(${targetDate.iso})를 5개 소스에서 교차 검증

종목 1 전일 종가 검증 (5개 소스 필수):

🔍 소스1 (네이버 금융 - 최우선):
검색어: "[종목명] [6자리코드] ${searchFormats.naverStyle} 종가 site:finance.naver.com"
결과: ___원 (거래일: ${targetDate.iso})

🔍 소스2 (한국거래소 공식):
검색어: "[6자리코드] ${targetDate.numeric} 종가 site:data.krx.co.kr"
결과: ___원 (거래일: ${targetDate.iso})

🔍 소스3 (다음 금융):
검색어: "[종목명] ${searchFormats.isoStyle} 주가 site:finance.daum.net"
결과: ___원 (거래일: ${targetDate.iso})

🔍 소스4 (네이버 증권 시세):
검색어: "[종목명] 종가 ${searchFormats.naverStyle} site:finance.naver.com/item"
결과: ___원 (거래일: ${targetDate.iso})

🔍 소스5 (investing.com):
검색어: "[종목명] [6자리코드] korea stock ${searchFormats.isoStyle} close site:investing.com"
결과: ___원 (거래일: ${targetDate.iso})

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 검증 로직 (강화된 다수결 원칙):

STEP 3-1: 이상치 제거
- 5개 소스 중 최댓값, 최솟값 1개씩 제거
- 남은 3개 값으로 중앙값(median) 계산
- 중앙값을 "검증 기준값"으로 설정

STEP 3-2: STAGE 4 데이터와 비교
검증 기준값: ___원
STAGE 4 데이터: ___원
차이율: abs((STAGE4 - 기준값) / 기준값) × 100 = ___%

STEP 3-3: 최종 판정

IF 차이율 ≤ 0.5%:
   ✅✅✅ 검증 완벽 통과 (오차 0.5% 이내)
   → STAGE 4 데이터 그대로 사용

ELSE IF 차이율 ≤ 1.0%:
   ✅ 검증 통과 (오차 1.0% 이내, 소스 간 시간차 허용)
   → 검증 기준값(중앙값)으로 대체
   ⚠️ 로그 기록: "[종목명] 전일 종가 보정: STAGE4(__원) → 기준값(__원)"

ELSE IF 차이율 ≤ 2.0%:
   ⚠️⚠️ 경고: 2% 이내 불일치 감지
   → 5개 소스 재확인 후 최다 합의값 사용
   → 3개 이상 소스가 동일값이면 해당 값 사용
   → 그렇지 않으면 종목 교체

ELSE (차이율 > 2.0%):
   ❌❌❌ 전일 종가 검증 실패 (차이 > 2%)
   → 5개 소스 재검색 1회 더 시도
   → 재시도 후에도 실패 시 해당 종목 교체
   → 교체 종목 = STAGE 4 예비 후보(4위~10위) 중 최상위
   → 교체 후 새 종목에 대해 STEP 2~3 전체 재검증

종목 2 전일 종가 검증: (동일 5개 소스 검증 프로세스 반복)
종목 3 전일 종가 검증: (동일 5개 소스 검증 프로세스 반복)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚨 검증 실패 시 복구 절차 (단계별 Failover 전략):

🔴 PHASE 1: 실패 원인 분류 (Root Cause Analysis)

실패 유형 A: 소스 부족 (5개 소스 중 3개 미만만 데이터 반환)
→ 조치: 검색어 변형 후 재시도 (종목코드 기반, 점 형식 날짜 "${searchFormats.dotStyle}" 활용)

실패 유형 B: 가격 불일치 (5개 소스 있으나 차이율 > 2%)
→ 조치: 추가 소스 확보 후 재검증
  * site:stockplus.com / site:38.co.kr / site:hankyung.com

실패 유형 C: 날짜 불일치 (검색 결과의 거래일이 ${targetDate.iso}가 아님)
→ 조치: 검색어에 날짜 포맷을 바꿔 재시도 ("${searchFormats.krxStyle}", "${searchFormats.dotStyle}")
→ ⚠️ 전일 영업일을 직접 재계산하지 마세요 — 기준일은 ${targetDate.iso}로 고정입니다

🟡 PHASE 2: 재시도 전략 (종목별 최대 3회)

【1차 시도】 초기 검증: 5개 소스 검증 실행 → 실패 시 유형 분류
【2차 시도】 강화된 재검증: 실패 유형별 대응 (추가 소스 3개, 총 8개 소스 중 5개 확보 목표)
【3차 시도】 최종 재검증: 다른 검색어 조합 + 영문 소스 (bloomberg.com, reuters.com)

🟢 PHASE 3: 종목 교체 프로세스 (3회 재시도 실패 시)

현재 종목: 검증 3회 실패 → 교체 대상 확정
로그 기록: "[종목명] 전일 종가 검증 불가 (실패 유형: __)"

【교체 후보 선정】
STAGE 4 예비 후보(4위~10위)에서 순서대로 선택:
- 교체 시: 4위 → 5위 → 6위 → ... → 10위 순서
- 예비 후보에 없는 종목으로 교체 금지 (환각 방지)

【교체 종목 검증】
새 종목에 대해 전체 프로세스 재실행:
✅ STEP 2: 종목 실존 여부 검증
✅ STEP 3: 전일 종가 5개 소스 검증
✅ 성공 시 → 최종 3개 종목 명단에 포함
❌ 실패 시 → 다음 순위 종목으로 재교체

🔵 PHASE 4: 전체 복구 시도 제한 및 최종 실패 처리

【카운터 추적】
- 재시도 카운터: 각 종목당 최대 3회
- 교체 카운터: 전체 최대 7회 (예비 후보 4위~10위 소진 시까지)

【중간 성공 처리】
IF 3개 종목 중 1~2개만 검증 성공:
   ✅ 성공한 종목은 확정 보존
   🔄 실패한 종목만 교체 프로세스 진행

【최종 실패 조건】
IF 예비 후보 7개를 모두 소진해도 3개 종목 확보 실패:
   ❌ JSON 생성하지 말 것
   ❌ 에러 메시지만 출력: "STAGE 5 실패: 3개 종목 확보 불가 (재시도 __회, 교체 __회, 성공 __개)"
   ❌ 1~2개 종목만으로는 절대 JSON 출력 금지

⚠️ 빈 JSON [] 절대 금지
⚠️ 3개 미만 JSON 절대 금지

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【STEP 4】 🔴 핵심 검증 #3: JSON 형식 정확성 (MANDATORY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

JSON 생성 전 체크리스트:

✅ 정확히 3개 종목 (더 많거나 적으면 안 됨)
✅ ticker 형식: "KOSPI:6자리코드" 또는 "KOSDAQ:6자리코드"
✅ 각 종목마다 필수 필드 5개 존재:
   - ticker (문자열)
   - name (문자열)
   - close_price (정수)
   - rationale (문자열, "|" 구분, 최소 12개 지표)
   - signals (객체, 7개 점수 모두 0-100 정수)

✅ 금지 필드 절대 포함 금지:
   - target_price ❌
   - entry_price ❌
   - stop_loss ❌
   - 투자 권유 표현 ❌

✅ JSON 문법:
   - '[' 로 시작
   - ']' 로 종료
   - 올바른 쉼표, 따옴표, 괄호
   - 유효한 JSON (파싱 가능)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【STEP 5】 최종 JSON 생성 및 출력
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ STEP 2 (종목 실존) 검증 완료
✅ STEP 3 (전일 종가) 검증 완료
✅ STEP 4 (JSON 형식) 검증 완료

→ 이제 JSON 출력 가능

출력 형식 (구조 예시 — 아래 값들은 형식 설명용이며, 실제 값은 반드시 검색으로 확인된 값만 사용):

[
  {
    "ticker": "[KOSPI 또는 KOSDAQ]:[6자리코드]",
    "name": "[검증된 종목명]",
    "close_price": [검색으로 확인된 정수값],
    "rationale": "[지표1 상태]|[지표2 상태]|...|[지표12+ 상태]",
    "signals": {
      "trend_score": [0-100 정수],
      "momentum_score": [0-100 정수],
      "volume_score": [0-100 정수],
      "volatility_score": [0-100 정수],
      "pattern_score": [0-100 정수],
      "sentiment_score": [0-100 정수],
      "overall_score": [0-100 정수]
    }
  },
  { ... 종목 2 동일 구조 ... },
  { ... 종목 3 동일 구조 ... }
]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔥 실행 명령

1. STEP 2 실행: 3개 종목 실존 여부 Google Search로 확인
2. STEP 3 실행: 3개 종목 전일 종가를 5개 소스에서 교차 검증 (기준일: ${targetDate.iso})
3. STEP 4 실행: JSON 형식 체크리스트 확인
4. 모든 검증 통과 시에만 STEP 5 실행: 순수 JSON 출력

⚠️ 검증 실패 시:
- JSON 생성하지 말 것
- 에러 메시지 출력: "검증 실패: [구체적 이유]"

⚠️ 검증 성공 시:
- '[' 로 시작하는 순수 JSON만 출력
- 다른 텍스트 일절 없음
- 완벽한 3개 종목 데이터

지금 즉시 STEP 2부터 순차 실행 시작!`;
}