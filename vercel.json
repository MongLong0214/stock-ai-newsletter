{
  "ignoreCommand": "base=${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}; git rev-parse --verify \"$base\" >/dev/null 2>&1 || exit 1; test -z \"$(git diff --name-only \"$base\" HEAD -- . ':(exclude,glob)docs/**' ':(exclude,glob)*.md' ':(exclude,glob)**/*.md' ':(exclude,glob).github/**' ':(exclude,glob).serena/**' ':(exclude,glob)mcp/**')\""
}
