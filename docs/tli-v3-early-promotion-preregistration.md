# TLI v3 M1 조기 승격 사전 등록 (Pre-registration)

Status: registered 2026-07-07 11:55 KST (리플레이 감사 실행 **전** 등록 — 결과를 보고 기준을 바꾸지 않기 위한 박제)
Authority: Isaac 전권 위임 (2026-07-07), CTO 판단
Scope: **최초 M1 승격 1회에 한함.** 이후 모든 승격은 PRD §8 표준 gate(4주 prospective shadow)로 복귀한다.

## 1. 결정

PRD §8의 "4주 shadow 후 승격"을 최초 승격에 한해 **progressive delivery**로 대체한다:

```
PIT 리플레이 감사 (오늘) → 통과 시 당일 champion 승격 + 실험(beta) 라벨 노출
→ 라이브 트립와이어 상시 감시 → D+14 확인 체크포인트 (통과=확정 / 미달=자동 롤백)
```

## 2. 근거 (왜 이것이 무모하지 않은가)

1. **현직 champion(b-abl-v1) 자체가 prospective 검증된 적 없는 휴리스틱**이며, offline 실측에서 ECE 0.2437(심각한 미보정), IC -0.0571(GT-A 역방향 예측)로 나타났다. 4주 gate의 보수성은 "검증된 champion을 근소한 challenger로부터 보호"하기 위한 설계인데, 지금은 그 전제가 성립하지 않는다.
2. **효과 크기가 gate 설계 범위 밖으로 크다**: 상대 개선 22.3% (gate 최소 기준 2%의 11배), 99% CI [-0.0741, -0.0353]로 0에서 결정적으로 이탈, 비중복 n=1,751 (최소 기준의 7배).
3. **되돌리기가 싸다**: 롤백 RPC(038) + 노출 플래그(`TLI_PREDICTIONS_V3_EXPOSURE_ENABLED`) 이중 안전망. 실패 시 피해 반경 = UI 확률 배지 1개 (점수 정렬은 Q6 결정대로 유지되므로 불변).
4. **남은 유일한 실질 리스크 = offline→serving 전이 버그** (누수, 아티팩트 직렬화, 서빙 경로 불일치). 이것이 리플레이 감사가 정확히 겨냥하는 대상이다.

## 3. 리플레이 감사 통과 기준 (변경 불가)

train ≤ 2026-05-29 아티팩트로 2026-06-08~06-26을 일 단위 PIT 재생, final GT-A 라벨로 채점:

| # | 기준 | 임계값 |
|---|---|---|
| C1 | M1 Brier < B-abl Brier (point) | 필수 |
| C2 | M1 Brier 절대값 | ≤ 0.21 |
| C3 | M1 ECE | ≤ 0.08 |
| C4 | M1 IC | > 0 |

4개 전부 충족 시에만 pass. 하나라도 미달이면 승격하지 않고 표준 4주 gate로 복귀한다.

## 4. 통과 시 당일 실행 항목

1. `promote_model_registry_version('m1-2026w28')` — champion 승격 (b-abl-v1은 shadow로 계속 기록되어 비교 대상 유지)
2. `TLI_PREDICTIONS_V3_EXPOSURE_ENABLED=true` — 확률 배지 노출, UI에 **"실험(beta)"** 라벨 필수
3. 본 문서에 실행 시각·증빙 링크 추가

## 5. 라이브 트립와이어 (일일 감시, 위반 시 즉시 조치)

| # | 조건 | 조치 |
|---|---|---|
| T1 | 라이브 비중복 라벨 ≥60개 시점부터, M1 누적 Brier > 동일 행 B-abl(shadow) Brier | **롤백** (RPC 038) |
| T2 | 라이브 라벨 ≥100개 시점부터, M1 ECE > 0.15 | **롤백** |
| T3 | reflexivity report에서 오염 신호 (자기 발행→검색 상관) | **노출 플래그 off** (champion 유지, 원인 조사) |
| T4 | 예측 기록 실패 2일 연속 (파이프라인 건강) | **롤백** + Issue |

## 6. D+14 확인 체크포인트 (2026-07-21)

- 라이브 예측(7/8~7/21) vs 동일 행 B-abl shadow, theme-cluster bootstrap 99% Brier delta CI.
- **통과** (upper < 0): 승격 확정. 기록용으로 4주 시점 표준 gate 리포트도 생성.
- **미달** (upper ≥ 0): 자동 롤백 + 포스트모템. 이후 표준 4주 gate 경로만 사용.
- α-회계: 사전 등록된 interim look 1회 추가로 family-wise α 증가는 ~2% 수준. 이 look은 "승격을 앞당기는" 방향이 아니라 "이미 승격된 것을 되돌리는" 방향의 안전장치이므로 승격 방향 α-inflation은 없다.

## 7. 실행 기록

- 2026-07-07: 사전 등록 (리플레이 감사 결과 미확인 상태에서 작성) — 이 커밋의 타임스탬프가 증거.
- (이후 결과 추가)
